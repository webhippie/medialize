package config

var (
	// Update determines if automated updates are enabled.
	Update bool

	// Debug defines if we want to print debug log messages.
	Debug bool

	// Rename defines if photos should be renamed or only copied.
	Rename bool
)
